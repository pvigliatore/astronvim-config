---
description: Coding standards for the current project.
alwaysApply: true
---

# Coding Standards

If the file `.editorconfig` is present, ensure that changes are always consistent with the rules defined in that file.
In particular, adding or removing trailing newlines should not conflict with the rules defined in `.editorconfig`.
